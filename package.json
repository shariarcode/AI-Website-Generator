{
  "name": "ai-website-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --loader:.tsx=tsx --define:process.env.API_KEY=\\\"$API_KEY\\\" --minify --sourcemap"
  },
  "devDependencies": {
    "esbuild": "^0.21.4"
  }
}
